llscheme - A Scheme to LLVM Compiler
====================================

Dependencies
------------
1. LLVM C++ API and tools, http://llvm.org/
2. BDW-GC library, http://www.hpl.hp.com/personal/Hans_Boehm/gc/
3. GMP library, http://gmplib.org/

llscheme is developed with LLVM 2.6 and 2.7, and we are not sure
whether it will compile on other versions.


Installation
------------

This source archive uses the GNU autotools to build. Ensure that the
standard GNU build environment is available, including autoconf,
automake, etc.

To build:

    ./autogen.sh
    make
    sudo make install

Features:
--------
* Supported special form: define, lambda, if, quote, begin
* Supported data types: number, string, list

A list of implemented built-in procedures:
1. Arithmetic
   +, -, *, /, =, <, >, <=, >=
   number?, complex?, real?, rational?, integer?, exact?, inexact?

2. Pair and List
   cons, car, cdr, set-car!, set-cdr!, list, length, list-ref
   null?, pair?, list?

3. String and Symbol
   string?, symbol?, string=?, string-ci=?, string-length, substring,
   string-append, string->number

4. I/O
   display

TODO
----
3. Implement let/let*/letrec;
5. Basic type inference system;
6. Implement set!, I/O sub-routines;
7. Continuations;
8. Hygienic macro.
